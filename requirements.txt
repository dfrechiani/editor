anthropic==0.2.7  # Substitua pela versão mais recente estável
streamlit==1.26.0  # Exemplo da versão do Streamlit (confirme a sua)
elevenlabs==0.2.16
python-dotenv==1.0.0  # Caso esteja usando para gerenciar variáveis de ambiente

